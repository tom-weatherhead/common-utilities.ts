{
	"name": "thaw-common-utilities.ts-test-js",
	"version": "0.0.0",
	"description": "Test the use of thaw-common-utilities.ts in a JavaScript application",
	"main": "src/main.js",
	"scripts": {
		"clean": "rm -rf node_modules && rm -f package-lock.json",
		"rebuild": "npm run clean && npm i && npm run start",
		"start": "node src/main.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"thaw-common-utilities.ts": "file:../../thaw-common-utilities.ts-0.0.0.tgz"
	},
	"keywords": [],
	"author": "Tom Weatherhead <null@2hrd4u.org> (https://httpbin.org/status/418)",
	"license": "MIT"
}
