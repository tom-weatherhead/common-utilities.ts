{
	"name": "thaw-common-utilities.ts-test-ts",
	"version": "0.0.0",
	"description": "Test the use of thaw-common-utilities.ts in a TypeScript application",
	"main": "lib/main.js",
	"scripts": {
		"clean": "rm -rf node_modules && rm -f package-lock.json",
		"build": "tsc",
		"rebuild": "npm run clean && npm i && npm run build && npm run start",
		"start": "node lib/main.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"thaw-common-utilities.ts": "file:../../thaw-common-utilities.ts-0.0.0.tgz",
		"typescript": "^3.6.4"
	},
	"devDependencies": {
		"@types/node": "^12.12.5"
	},
	"keywords": [],
	"author": "Tom Weatherhead <null@2hrd4u.org> (https://httpbin.org/status/418)",
	"license": "MIT"
}
